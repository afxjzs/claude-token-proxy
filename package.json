{
  "name": "claude-token-proxy",
  "version": "1.0.0",
  "description": "Proxy service that manages Claude OAuth token refresh and forwards API requests",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "pino": "^9.6.0",
    "proper-lockfile": "^4.1.2",
    "undici": "^7.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/proper-lockfile": "^4.1.4",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
